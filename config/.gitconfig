[user]
	name = Shuo Wang
	email = ws18022@outlook.com
[alias]
	st = status
	addcommit = !git add -A && git commit -m
	pullmain = pull origin main
	newbranch = checkout -b 
	discard = reset --hard
[core]
	excludesfile = /Users/wangshuo/.gitignore_global

[diff]
	tool = ksdiff
	
[difftool]
    prompt = false

[difftool "ksdiff"]
	cmd = ksdiff  --partial-changeset "$LOCAL" "$REMOTE"
    prompt = false

